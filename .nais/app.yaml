apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  name: nav-mangfold-frontend      
  namespace: heda                   
  labels:
    team: heda
spec:       
  port: 3000                      
  ingresses:
    - https://intern.dev.nav.no/mangfold
  replicas:
    min: 2
    max: 4
  resources:
    requests:
      cpu: 500m
      memory: 512Mi
  #Legger de senere når vi har de klærer
  # liveness:
  #  path: /
   # initialDelay: 20
  #readiness:
   # path: /
    #initialDelay: 20